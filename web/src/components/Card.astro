---
interface Props {
  title: string;
  isOpen: boolean;
}

// Use TypeScript in this fenced block
const props: Props = Astro.props;
---

<article>
	<details {...(props.isOpen? { open: true} : {})}>
		<summary>{props.title}</summary>
		<slot/>
	</details>
</article>


<style>

</style>
